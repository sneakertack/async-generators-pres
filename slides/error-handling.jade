section.slide
  h1.text-center Error handling *could work

  pre
    code.language-javascript.
      co(function *() {
        try {
          var products = yield tryHacking('amazon.com'); // Expected to fail
          res.send(products);
        } catch (e) {
          console.log(e.message);
        }
      })();

  p.text-center: small The co library: <a href="https://github.com/visionmedia/co" target="_blank">https://github.com/visionmedia/co</a>
