section.slide
  //- Using tables for layout. Kindly deal with it.
  table.full-width
    tr
      td(style="width: 50%;")
        code
          (function () {
            var products = loadProductsFromDatabase();
            res.send(products);
          })();
      td(style="width: 50%;")
        p a something something